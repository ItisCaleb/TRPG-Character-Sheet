<div class="right-menu pop" style="display: none">
    <img style="width: 50%" src="/public/source/toaster.svg" alt="">
    <a class="btn" id="btn" onclick="get('/')">首頁</a>
    <a class="btn" onclick="get('/about')">關於我們</a>
    <%const userData = authData || {
        auth:false
    }%>
    <%if(userData.auth===true) {%>
            <a class="btn " onclick="get('/trpgsession')" >團務</a>
            <a class="btn " onclick="get('/charactersheet')" >角色卡管理</a>
            <a class="btn adminpost" onclick="get('/adminpost')">管理員貼文</a>
        <%}%>
    <%if (userData.auth===true) {%>
        <a href='/user'>歡迎!<%=authData.user%></a></button>
        <a class="btn exit">登出</a>
    <%}else {%>
        <a href='/login'>登入</a>
        <a href='/signup'>註冊</a>
    <%}%>
</div>