<!DOCTYPE html>
<html lang="en">
<head>
    <%include head.ejs%>
</head>
<body>
<nav>
<% include header.ejs%>
<div id="password-window" class="message pop" style="top: 24px;display: none">
        <div class="message-div">
            <div class="message-alert">更改密碼
                <div class="message-content">
                    <div style="text-align: center">
                        <form id="password_input" action="../api/user/password" method="post">
                            <label class="table-bar" for="old_password" style="font-size: 15px"><b>輸入舊密碼</b></label><br>
                            <input type="password" placeholder="輸入舊密碼" name="old_password" id="old_password" required><br>
                            <label class="table-bar" for="new_password" style="font-size: 15px"><b>輸入新密碼</b></label><br>
                            <input type="password" placeholder="輸入新密碼" name="new_password" id="new_password" required><br>
                            <button class="btn session" type="submit">確認</button>
                        </form>
                        <button class="btn session" id="cancel-password">取消</button>
                    </div>
                </div>
            </div>
        </div>
    </div>
</nav>
<% include rightmenu.ejs%>
<div class="content">
    <h1 style="text-align: center;line-height:40px ;"><%=title%></h1>
    <div class="content-decribe" style="text-align: center;line-height:30px ;">
        這邊應該顯示個人主頁
    <div>電子郵件/帳號:<%=email%></div>
    <div>角色卡數量:<%=number%>張</div>
    <button class="btn session" id="change-password">修改密碼</button>
    </div>
</div>
<script>
    $('#password_input').submit(function (e) {
        e.preventDefault();

        $.ajax({
            url: $(this).attr("action"),
            type: 'POST',
            data:$(this).serializeArray() ,
            success:function(data){
                good_message(data)
                setTimeout(function(){
                    redirect('/user')
                },1000)
            },
            error:function(data){
                bad_message(data.responseText)
            },
        });
    })
</script>
<%include footer.ejs%>
</body>
</html>